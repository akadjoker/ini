INI File Parser

This project is an implementation of a parser for INI (Initialization) files. It provides a simple and efficient way to parse and extract key-value pairs from INI configuration files commonly used for storing application settings and configurations.
Overview

The project consists of the following components:

    Lexer: The lexer component tokenizes the input INI file into meaningful tokens, such as sections, keys, values, and comments. It handles whitespace and ignores irrelevant characters.